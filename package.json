{
  "name": "spec-ingest-tool",
  "version": "0.1.0-demo",
  "private": true,
  "description": "Spec ingest tool for extracting candidates, building a merged corpus, and generating implementation artifacts.",
  "type": "module",
  "bin": {
    "spec-ingest": "./cli.mjs"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "lint": "eslint .",
    "mcp": "node mcp.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "eslint": "^9.9.0",
    "globals": "^15.9.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^4.1.11"
  }
}
