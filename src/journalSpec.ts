import type { Candidate } from './profiles/types.js'

// DEMO/REFERENCE SCAFFOLD — see Spec-Ingest-Tool.md section 6 (reading a
// recorded session: meta.json, fields.json, ax-tree.json, styles.json).
export async function readRecordedSession(_dir: string): Promise<Candidate[]> {
  return []
}
