# DEMO/REFERENCE SCAFFOLD -- see ../Console.md section 3: BrowserRouter
# needs this fallback or a deep link 404s on a static server.
server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
