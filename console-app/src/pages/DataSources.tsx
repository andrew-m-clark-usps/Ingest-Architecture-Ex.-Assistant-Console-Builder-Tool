import { Typography } from '@mui/material'

// DEMO/REFERENCE SCAFFOLD -- ../Console.md section 4 item 8 (data sources,
// the ingest log).
export function DataSources() {
  return (
    <>
      <Typography variant="h4" gutterBottom>
        Data sources
      </Typography>
      <Typography color="text.secondary">
        Demo scaffold: drag-and-drop ingest and the rejected-row log are not
        implemented -- see ../Console.md section 4.
      </Typography>
    </>
  )
}
