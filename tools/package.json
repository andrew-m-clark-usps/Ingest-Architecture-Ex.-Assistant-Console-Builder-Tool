{
  "name": "assistant-tools",
  "version": "0.1.0-demo",
  "private": true,
  "description": "tools/ may use whatever it needs (Exec-Assistant.md invariants 6/7 do not bind it). Playwright and the MCP SDK live here, not in the assistant's rendered pages.",
  "type": "module",
  "scripts": {
    "twin": "node twinning.mjs",
    "twin:mcp": "node twinning_mcp.mjs",
    "lint": "eslint ."
  },
  "dependencies": {
    "playwright": "^1.47.0",
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "eslint": "^9.9.0",
    "globals": "^15.9.0"
  }
}
